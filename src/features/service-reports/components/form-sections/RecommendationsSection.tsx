import FormSection from "./FormSection";

export default function RecommendationsSection() {
    return (
        <FormSection
            title="Recommendations"
            description="Add any recommendations or follow-up actions for the customer."
        />
    );
}